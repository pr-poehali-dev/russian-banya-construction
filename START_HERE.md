# 🔍 ПОИСК УТЕРЯННЫХ ПРОЕКТОВ

## Что случилось?

Проекты **"Красная слудка"** и **"Ключи"** были удалены из файла `src/components/home/projectData.ts` при последней декомпозиции.

## ⚡ Быстрое решение

### Запустите мастер-скрипт (всё в одном):

```bash
chmod +x master_search.sh
./master_search.sh
```

Этот скрипт:
- Использует 3 разных метода поиска
- Автоматически найдет и сохранит все версии файлов
- Создаст подробный отчет
- Сохранит результаты в `git_history_results/`

### Результат

Если проекты найдены, вы увидите:
```
✓✓✓ УСПЕХ! Проекты НАЙДЕНЫ!

Найденные файлы:
FOUND_krasnaya_abc12345.ts
FOUND_klyuchi_def67890.ts
```

## 📋 Альтернативные методы

Если мастер-скрипт не сработал, попробуйте:

### 1. Детальный поиск
```bash
chmod +x find_projects.sh
./find_projects.sh
```

### 2. Python анализ
```bash
python3 extract_projects.py
```

### 3. Продвинутый Git
```bash
chmod +x advanced_search.sh
./advanced_search.sh
```

### 4. Ручной поиск
```bash
# Поиск коммитов
git log -S"Красная" --all -- src/components/home/projectData.ts

# Просмотр конкретного коммита
git show COMMIT_HASH:src/components/home/projectData.ts
```

## 📖 Подробная документация

- `README_GIT_RECOVERY.md` - полное руководство по восстановлению
- `INSTRUCTIONS_RU.md` - подробные инструкции на русском

## ✅ После нахождения данных

1. Откройте файлы из `git_history_results/FOUND_*.ts`
2. Найдите нужные проекты:
   ```typescript
   {
     img: "URL",
     title: "Название",
     size: "Размер",
     material: "Материал",
     location: "Локация"
   }
   ```
3. Найдите соответствующие галереи в `projectGalleries`
4. Добавьте данные обратно в `projectData.ts`

## 🆘 Что делать, если ничего не найдено?

1. Проверьте другие ветки: `git branch -a`
2. Поищите в других файлах: `grep -r "Красная" src/`
3. Проверьте reflog: `git reflog`
4. Возможно, проекты были в другом файле или репозитории

## 🛠️ Отладка

Если скрипты не запускаются:

```bash
# Проверка Git
git --version

# Проверка репозитория
git status

# Проверка истории файла
git log --oneline -- src/components/home/projectData.ts
```

---

## 🚀 Начните прямо сейчас!

```bash
chmod +x master_search.sh && ./master_search.sh
```

Удачи! 🍀
