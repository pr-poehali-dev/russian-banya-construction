# Восстановление проектов "Красная слудка" и "Ключи" из Git истории

## Проблема

Два проекта были удалены из `src/components/home/projectData.ts`:
- **"Красная слудка"**
- **"Ключи"**

Нужно найти их в git истории и восстановить полные данные.

## Быстрый старт

### Способ 1: Автоматический поиск (РЕКОМЕНДУЕТСЯ)

```bash
chmod +x find_projects.sh
./find_projects.sh
```

Результаты появятся в папке `git_history_results/`

### Способ 2: Python анализ

```bash
python3 extract_projects.py
```

### Способ 3: Продвинутый Git поиск

```bash
chmod +x advanced_search.sh
./advanced_search.sh
```

## Ручной поиск

Если автоматические скрипты не работают, используйте следующие команды:

### 1. Поиск коммитов с изменениями

```bash
# Поиск по содержимому (pickaxe)
git log -S"Красная" --all -- src/components/home/projectData.ts
git log -S"Ключи" --all -- src/components/home/projectData.ts

# Поиск с регулярными выражениями
git log -G"[Кк]расная" --all -- src/components/home/projectData.ts
git log -G"[Кк]лючи" --all -- src/components/home/projectData.ts
```

### 2. Просмотр содержимого файла в коммите

Когда найдете нужный коммит (например, `abc12345`):

```bash
# Показать содержимое файла
git show abc12345:src/components/home/projectData.ts

# Сохранить в файл
git show abc12345:src/components/home/projectData.ts > found_version.ts
```

### 3. Просмотр изменений с контекстом

```bash
# Показать diff с контекстом
git log -p -S"Красная" -- src/components/home/projectData.ts

# Показать только строки с изменениями
git log -p -S"Красная" -- src/components/home/projectData.ts | grep -A 10 -B 10 -i "красная"
```

### 4. Поиск во всех ветках

```bash
# Список веток
git branch -a

# Поиск в конкретной ветке
git log -S"Красная" origin/main -- src/components/home/projectData.ts

# Поиск во всех ветках
git log -S"Красная" --all -- src/components/home/projectData.ts
```

## Структура данных для восстановления

### Данные проекта (из массива `projects`)

```typescript
{
  img: string,           // URL главного фото
  title: string,         // Название проекта
  size: string,          // Размер, например "6×8 м"
  material: string,      // Материал, например "Профилированный брус"
  location: string       // Локация, например "д. Красная Слудка, Пермский край"
}
```

### Галерея проекта (из массива `projectGalleries`)

```typescript
[
  "https://cdn.poehali.dev/files/photo1.jpg",
  "https://cdn.poehali.dev/files/photo2.jpg",
  // ... все фотографии проекта
]
```

## Что делать после нахождения

1. **Откройте найденный файл** из `git_history_results/` или сохраненный вручную

2. **Найдите блок с проектом**:
   ```typescript
   {
     img: "...",
     title: "Красная слудка" / "Ключи",
     size: "...",
     material: "...",
     location: "..."
   }
   ```

3. **Найдите соответствующий массив фотографий** в `projectGalleries`
   - Проекты в `projects` и галереи в `projectGalleries` идут в том же порядке
   - Если проект был 3-м в списке, то его галерея - это `projectGalleries[2]`

4. **Скопируйте данные** в текущий `projectData.ts`

5. **Проверьте**, что все URL фотографий рабочие

## Альтернативные методы

### Использование GitHub (если есть доступ)

1. Откройте репозиторий на GitHub
2. Перейдите в файл `src/components/home/projectData.ts`
3. Нажмите "History"
4. Просмотрите старые версии файла

### Использование Git GUI

```bash
# Запустить gitk для визуального просмотра
gitk -- src/components/home/projectData.ts

# Или Git GUI
git gui blame src/components/home/projectData.ts
```

### Поиск в reflog

```bash
# Просмотр reflog
git reflog

# Поиск по конкретному файлу
git log --walk-reflogs -- src/components/home/projectData.ts
```

## Проверка результатов

После восстановления данных проверьте:

1. ✓ Все поля проекта заполнены (title, size, material, location, img)
2. ✓ Главное фото (img) доступно
3. ✓ Все фотографии в галерее доступны
4. ✓ Порядок проектов и галерей соответствует
5. ✓ Нет синтаксических ошибок в TypeScript

## Отладка

Если скрипты не работают:

```bash
# Проверка наличия git
git --version

# Проверка текущего репозитория
git status

# Проверка истории файла
git log --oneline -- src/components/home/projectData.ts

# Проверка всех коммитов
git log --all --oneline --graph

# Проверка удаленных веток
git branch -r
```

## Контакты для помощи

Если возникли проблемы:
1. Проверьте логи скриптов
2. Попробуйте альтернативные методы
3. Обратитесь к администратору репозитория

## Примечания

- Все скрипты безопасны - только читают данные
- Git история не изменяется
- Можно запускать несколько раз
- Результаты сохраняются в отдельную папку

---

Удачи в восстановлении! 🔍
